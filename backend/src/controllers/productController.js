exports.getProducts = (req, res) => {
  res.status(200).json({ message: 'Product list endpoint placeholder' });
};

exports.createProduct = (req, res) => {
  res.status(201).json({ message: 'Product create endpoint placeholder' });
};
