exports.getCustomers = (req, res) => {
  res.status(200).json({ message: 'Customer list endpoint placeholder' });
};

exports.createCustomer = (req, res) => {
  res.status(201).json({ message: 'Customer create endpoint placeholder' });
};
